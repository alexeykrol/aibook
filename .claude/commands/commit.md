# /commit — Git commit для литературного проекта

Используй эту команду для создания осмысленных коммитов.

## Типы коммитов для литературы:

| Префикс | Когда использовать | Пример |
|---------|-------------------|--------|
| `content:` | Новый контент (главы, уровни) | `content: добавлен уровень 1` |
| `edit:` | Редактура существующего текста | `edit: правки в предисловии` |
| `meta:` | Мета-файлы (BACKLOG, SNAPSHOT) | `meta: обновление после сессии` |
| `structure:` | Изменение структуры проекта | `structure: реорганизация папок` |
| `template:` | Шаблоны | `template: добавлен шаблон кейса` |
| `docs:` | Документация (README, CLAUDE.md) | `docs: обновлен README` |
| `fix:` | Исправление ошибок | `fix: опечатка в главе 3` |

## Что делать:

### 1. Проверь статус:

```bash
git status
git diff --stat
```

### 2. Определи тип изменений:

- Что изменилось? (контент/структура/мета)
- Какой префикс подходит?

### 3. Сформируй сообщение:

```
[префикс]: [краткое описание на русском]

[опционально: детали в теле коммита]
```

### 4. Создай коммит:

```bash
git add .
git commit -m "[сообщение]"
```

## Примеры хороших коммитов:

```
content: добавлен уровень 1 — Врата Джина

- Теория: промпты, контекст, роли
- 3 квеста с заданиями
- 2 кейса (бизнес, творчество)
```

```
meta: обновление после сессии 2025-11-25

- BACKLOG: Phase 1 завершена (100%)
- SNAPSHOT: переход к Phase 2
```

```
edit: правки в предисловии по комментариям автора
```

## Плохие коммиты (избегай):

❌ `update` — непонятно что обновлено
❌ `fix` — непонятно что исправлено
❌ `wip` — незавершённая работа не должна коммититься
❌ `changes` — бессмысленно

## Автоматическая подпись:

Все коммиты автоматически подписываются:
```
🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```
