# /sync-git — Синхронизация с GitHub

Синхронизирует локальный репозиторий с GitHub (fetch + pull).

## Что делать:

1. **Fetch с remote:**
   ```bash
   git fetch origin
   ```

2. **Проверь статус:**
   ```bash
   git status
   ```

3. **Если есть новые коммиты — покажи их:**
   ```bash
   git log --oneline HEAD..origin/main
   ```

4. **Подтяни изменения:**
   ```bash
   git pull origin main
   ```

5. **Покажи результат:**
   ```
   ✅ Синхронизировано!

   Получено X коммитов:
   - [краткое описание изменений]

   Текущий коммит: [hash]
   ```

   Или если уже синхронизировано:
   ```
   ✅ Уже актуально — новых коммитов нет
   ```

## Обработка конфликтов:

Если есть локальные изменения, которые конфликтуют:

1. Сообщи автору о конфликте
2. Предложи варианты:
   - Stash локальных изменений
   - Commit сначала
   - Посмотреть diff

## Пример вывода:

```
🔄 Синхронизация с GitHub...

📥 Найдено 3 новых коммита:
- abc1234 content: добавлен уровень 2
- def5678 edit: правки введения
- ghi9012 meta: обновлён BACKLOG

✅ Синхронизировано!
Текущий коммит: ghi9012
```
